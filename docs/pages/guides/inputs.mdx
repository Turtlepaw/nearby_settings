import { Steps, Tabs } from "nextra/components";

# Inputs

<Tabs items={['Text Input', 'Number Input', 'Toggle Input', 'Select Input', 'Multi Select Input']}>
  <Tabs.Tab>

![Text Input](/inputs/text_input.png)

```kotlin
SettingSchema(
    key = "text_input",
    label = "Text Input",
    description = "**Markdown** is fully supported thanks to [flutter_markdown](https://pub.dev/packages/flutter_markdown)!",
    type = SettingType.TEXT,
    constraints = SettingConstraints(
        min = 5,
        max = 10
    )
)
```

</Tabs.Tab>
<Tabs.Tab>
  
![Number Input](/inputs/number_input.png)
```kotlin
SettingSchema(
    key = "number_input",
    label = "Number Input",
    type = SettingType.NUMBER,
    constraints = SettingConstraints(
        min = 1,
        max = 10
    )
)
```
</Tabs.Tab>
<Tabs.Tab>

![Toggle Input](/inputs/toggle_input.png)

```kotlin
SettingSchema(
    key = "toggle_input",
    description = "This is a toggle input",
    label = "Toggle Input",
    type = SettingType.TOGGLE,
)
```

</Tabs.Tab>
<Tabs.Tab>

![Select Input](/inputs/select_input.png)

```kotlin
SettingSchema(
    key = "select_input",
    label = "Select Input",
    type = SettingType.SELECT,
    constraints = SettingConstraints(
        options = listOf("option1", "option2", "option3"),
    )
)
```

</Tabs.Tab>
<Tabs.Tab>

![Multi Select Input](/inputs/multiselect_input.png)

```kotlin
SettingSchema(
    key = "multiselect_input",
    label = "Multiselect Input",
    type = SettingType.MULTI_SELECT,
    constraints = SettingConstraints(
        options = listOf("option1", "option2", "option3"),
        max = 2,
        min = 1
    )
)
```

</Tabs.Tab>
</Tabs>
